<%= @offer.title %>
<%= @offer.cuisine %>
<%= @offer.price_pp %>
<%= @offer.description %>
<%= link_to("Back to Offers", offers_path) %>

<%= simple_form_for([@offer, @booking]) do |f| %>
  <%= f.input :date %>
    <%= f.input :guests %>
  <%= f.submit %>
<% end %>
